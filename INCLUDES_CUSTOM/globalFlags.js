function globalFlags() {

if (matches(currentUserID,'RCARRILLO','JSOUSA','TDUNN','MHELVICK','THORNICK')) {
 showDebug = 3;
 } else {
 showDebug = false;
 /* enable debugging on on scripts for ADMIN userID */;
 }

}
